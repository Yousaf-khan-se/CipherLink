# Frontend Environment Variables (Development)
# Copy this to .env.local for local development

# API URL (leave empty for development - uses Vite proxy)
VITE_API_URL=

# Socket URL (leave empty for development - uses same origin)
VITE_SOCKET_URL=
